{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "gh-pages -d dist",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/node": "^18",
    "node-sass": "^9.0.0",
    "nuxt": "^3.5.2",
    "sass": "^1.62.1",
    "sass-loader": "^13.3.1"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
